[project]
name = "fastapilearning"
version = "0.1.0"
description = "Stateless FastAPI backend for Supabase auth and prompt generation"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.136.1",
    "PyJWT[crypto]>=2.10.0",
    "python-dotenv>=1.0.0",
    "slowapi>=0.1.9",
    "uvicorn[standard]>=0.47.0",
]
